# Middleware

Each middleware directory owns upstream source references, version decisions, topology, storage,
resource profiles, health checks, backup/restore integration, upgrade/rollback procedures, and
fault-drill hooks. Production-like manifests must not use floating image tags.
