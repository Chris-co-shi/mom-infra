# Third-Party Notices

This repository will deploy and configure third-party infrastructure components. Their source code
is not copied into this repository unless explicitly documented.

Before the first deployable release, record the exact version, upstream URL, license, chart or image
digest, and any local modifications for every component in `config/component-versions.yaml`.
